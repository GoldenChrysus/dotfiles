- defaults:
  link:
    relink: true

- clean: ['~']

- link:
  ~/.config/ngrok/ngrok.yml: .config/ngrok/ngrok.yml
  ~/.config/powerline-shell/config.json: .config/powerline-shell/config.json
  ~/.ssh/config: .ssh/config
  ~/.gitconfig: .gitconfig
  ~/.zprofile: .zprofile
